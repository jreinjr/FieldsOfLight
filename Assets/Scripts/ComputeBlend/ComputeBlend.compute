// Each #kernel tells which function to compile; you can have many kernels
#pragma kernel Blend

Texture2D<float4> hitTex;
RWTexture2D<float4> blendTex;

[numthreads(8, 8, 1)]
void Blend(uint3 id : SV_DispatchThreadID) 
{
	float hit = hitTex[id.xy].w;

	blendTex[id.xy] = float4(hit.x, 1, 0, 0);
}
